# ==== NextAuth (OBLIGATORIO en Vercel) ====
# Genera el tuyo con:  openssl rand -base64 32
NEXTAUTH_SECRET=cambia-esto-por-un-secreto-largo-y-aleatorio
# URL pública de la app (en local: http://localhost:3000)
NEXTAUTH_URL=http://localhost:3000

# ==== IA opcional (si no se configuran, los chats usan el modo offline) ====
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4.1-mini
